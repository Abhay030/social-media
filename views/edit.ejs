<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
       <div class="flex w-full justify-end">
        <a class="bg-red-500 px-3 py-2 rounded text-sm inline-block mb-5" href="/logout">logout</a>
       </div>
        <h3 class="text-3xl mb-3">Edit your post</h3>
        <form action="/update/<%= post._id%>" method="post">
            <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-2" name="content" id="" placeholder="Do the changes"><%= post.content %></textarea>
            <input class="px-3 py-2 bg-yellow-500 block rounded-md mt-2 w-40" type="submit" value="update post">
        </form>
    </div>
</body>
</html>