<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
       <div class="flex w-full justify-end">
        <a class="bg-red-500 px-3 py-2 rounded text-sm inline-block mb-5" href="/logout">logout</a>
       </div>
        <h3 class="text-3xl"><span class="font-light">Hello</span> , <%= user.name %></h3>
        <h5 class="mb-5">You can create a new post </h5>
        <form action="/post" method="post">
            <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id="" placeholder="What's on your mind"></textarea>
            <input class="px-3 py-2 bg-blue-500 block rounded-md mt-2 w-40" type="submit">
        </form>

        <div class="posts mt-20 ">
            <h3 class="text-zinc-400">Your posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/2 p-3 border-[1px] mb-4 border-zinc-800 bg-zinc-700">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p class="tracking-tight text-sm"><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %></small>
                        <div class="btns flex gap-4 mt-1">
                            <a href="/like/<%= post._id %>" class="text-pink-500">
                                <%= post.likes.indexOf(user._id) === -1 ? "like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%= post._id %>" class="text-zinc-500">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>